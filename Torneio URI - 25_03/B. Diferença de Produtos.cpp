#include <stdio.h>
int main(){
	
	int A;
	int B;
	int C;
	int D;
	int DIFERENCA;
	
	scanf("%d%d%d%d", &A, &B, &C, &D);
	DIFERENCA = (A * B - C * D);
	
	printf("DIFERENCA = %d\n", DIFERENCA);
	
}
