#include <stdio.h>
int main()
 {
 	int X, i = 1;
	
	while(X != 0)
	 {
	 	i = 1;
	   	scanf("%d", &X);
 	    for(i == 1; i < X; i++)
 	     {
 	    	printf("%d ", i);
	     }
	  if(X != 0)
	    printf("%d\n", i);
     }   
   return 0;  
 }
